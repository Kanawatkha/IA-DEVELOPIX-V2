/**
 * @file types/index.ts
 * @description Local domain types and component interfaces for the Store feature module.
 */

export interface HeroCategory {
  id: number;
  name: string;
  isComingSoon: boolean;
  image: string;
}
